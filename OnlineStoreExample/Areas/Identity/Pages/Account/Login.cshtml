@page
@model LoginModel

@{
    ViewData["Title"] = "Войти";
}

<h1>@ViewData["Title"]</h1>
<div class="row">
    <div class="col-md-4">
        <section>
            <form id="account" method="post">
                <h2>Используйте аккаунт, чтобы войти.</h2>
                <hr />
                <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                    <label asp-for="Input.Email" class="form-label">Email</label>
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                    <label asp-for="Input.Password" class="form-label">Пароль</label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="checkbox mb-3">
                    <label asp-for="Input.RememberMe" class="form-label">
                        <input class="form-check-input" asp-for="Input.RememberMe" />
                        Запомнить меня?
                    </label>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary mb-3">Войти</button>
                </div>
                <div>
                    <p>
                        <a id="forgot-password" asp-page="./ForgotPassword">Забыли пароль?</a>
                    </p>
                    <p>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Зарегистрировать новый аккаунт</a>
                    </p>
                    <p>
                        <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Подтвердить Email</a>
                    </p>
                </div>
            </form>
        </section>
    </div>
    <div class="col-md-8 col-md-offset-2">
        <h2>Готовые решения.</h2>
        <hr />

        <p>Необязательно регистрировать новый аккаунт, чтобы протестировать возможности сайта.</p>
        <p>Можно использовать приготовленные заранее аккаунты:</p>
        <hr />
        <p>Аккаунт пользователя:</p> 
        <p>Email: <b>user@user.com</b></p>
        <p>Пароль: <b>Pass123!</b></p>
        <hr />
        <p>Аккаунт администратора:</p> 
        <p> Email: <b>admin@admin.com</b></p> 
        <p> Пароль: <b>Pass123!</b></p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}